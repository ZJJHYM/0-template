# @package _global_
batch_size: 32
model: mixer_attn_pooling
pooling_head: 8
pooling_dim: 512
mixer_layer: 1

modal_list:
  - video
  - audio
  - text

trainer:
  gpus:
    - 0
  val_check_interval: 50